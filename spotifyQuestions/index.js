const sortByStrings = require('./question1-sortByStrings')
const decodeString = require('./question2-decodeString') 
const changePossibilities = require('./question3-changePossibilities')

module.exports = {
  sortByStrings,
  decodeString,
  changePossibilities,
}